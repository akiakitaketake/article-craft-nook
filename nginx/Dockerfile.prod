# Dockerfile.prod (方式B: メイン設定を置換)
FROM nginx:latest

# ヘルスチェック用（ついでにAPTキャッシュを掃除）
RUN apt-get update && apt-get install -y curl vim sudo lsof && rm -rf /var/lib/apt/lists/*

# 任意：既定の conf.d を空にして事故を防止（任せる）
RUN rm -f /etc/nginx/conf.d/*

# 君のフル構成ファイルをメイン設定として配置
COPY nginx.conf /etc/nginx/nginx.conf

# JSON形式のCMDで起動
CMD ["nginx", "-g", "daemon off;"]

EXPOSE 80
